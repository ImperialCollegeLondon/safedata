linters: linters_with_defaults(
    cyclocomp_linter(complexity_limit = 40L), # TODO - this is silly, but not refactoring these right now
    commented_code_linter = NULL,  # TODO - remove when tests renabled
    indentation_linter(indent = 4L), 
    object_usage_linter = NULL  # TODO - Not recognizing internal variables
  )
